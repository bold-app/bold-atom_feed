Atom Feed plugin for Bold
=========================

The feed holds the most recent published posts and is located at
`/atom.xml`. The corresponding route is named `atom_feed`.
You can configure the number of latest posts to show.

